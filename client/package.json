{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "deploy:docs": "pnpm run build && rm -rf ../docs && mkdir -p ../docs && cp -R dist/. ../docs/",
    "preview": "vite preview",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "sql.js": "^1.10.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@types/sql.js": "^1.4.9",
    "typescript": "~5.9.3",
    "vite": "^8.0.0-beta.13"
  }
}
