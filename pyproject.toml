[project]
name = "kotobank-jitsu-scraper"
version = "0.1.0"
description = "Scrape Kotobank Jitsu dictionary keyword lists to SQLite."
requires-python = ">=3.14,<3.15"
dependencies = [
  "requests",
  "lxml",
]

[dependency-groups]
dev = [
  "ruff",
  "ty",
]
